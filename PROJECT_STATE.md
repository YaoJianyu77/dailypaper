# Verified implementation status

This file records evidence and gaps. [README.md](README.md) is the entry page; [Daily Paper Settings](DAILY_REPORT_PRODUCT_REQUIREMENTS.md) owns preferences; [AGENTS.md](AGENTS.md) owns execution rules.

## Implemented boundary

The production command uses `scripts/daily_pipeline.py`, shared stage prompts, and settings-derived output contracts. YAML is restricted to infrastructure; the model and strongest-supported-reasoning policy belongs to the six-section settings page. The old split quotas, expiring exclusions, abstract caps, fallback summaries, and missing full-analysis command have been removed from the generation path.

Five canonical skill folders are linked through `.agents/skills/`. Both native discovery and stage prompts use them; maintenance requests do not initiate report generation. Each daily invocation checks current official recommendations and account access, then selects the uniquely strongest supported reasoning setting using documented ordering evidence. Model names, default choices and list positions do not establish capability. The Codex app-server transport explicitly applies that configuration to every stage and subagent, verifies effective thread settings and CLI versions, rejects reroutes, disables provider fallback, and retains authentication and permission controls. A synthetic PDF and native subagent prove tool access before discovery. Selection evidence and effective settings are logged under ignored `state/logs/`; the preparation receipt freezes the configuration across stages and retries.

Unattended production, dry-run and retry threads explicitly use `approvalPolicy="never"`. Effective parent and child turn records verify that policy and the existing `workspaceWrite` sandbox with disabled network access. No additional writable roots, permission grants or escalation are accepted. Verification reads ordinary Codex turn records without resuming active children; global credentials and permission configuration are unchanged. A bounded read-only retry handles the observed startup race where Codex announces a thread before its session metadata has been flushed; missing or substituted settings still stop the run.

`bash scripts/install_local_cron.sh` installs or updates dependencies and manages one Cronie job using the existing runner. The settings currently specify 07:00 America/New_York. The installer preserves unrelated entries and restores their timezone; a whole-run lock prevents overlapping manual or scheduled invocations. Versioned local CLI installs avoid replacing active binaries on NFS.

`scripts/recommendation_history.py` reads the complete canonical ledger plus prior recommendation evidence in archived reports and the legacy index. Publication reconciles that evidence without resetting existing records. A prepared run is reserved, written, verified, and finalized under a stable date-based ID. Retries verify the same artifacts; a non-forced Git update prevents overwriting another clone's commits.

The earlier maintenance upgrade did not generate or migrate production reports, images, or history. The later, explicitly requested September 8 publication is recorded below. The explicitly requested local schedule was installed; credentials and permission settings were preserved. The obsolete manual workflows `daily.yml`, `github-models-smoke.yml`, and `manual-smoke.yml` were removed after checking their references and callers. Only the Pages deployment workflow remains; the local daily cron job is unchanged. Technical module responsibilities and isolated test commands are in [implementation notes](docs/implementation.md).

The static site now uses one JavaScript report reader at `/reader/?date=YYYY-MM-DD`. Builds copy original Markdown and derive navigation metadata and section spans; no per-date HTML pages are emitted. The generic 404 page redirects old dated links. The production renderer checks this same viewer offline, retaining Chromium sandboxing and denied external resources. The report sources, images, unified settings, Codex permissions, and daily schedule are preserved.

## Verification

- On the user's explicit request, the already reviewed September 8 draft was revalidated with a fresh real runtime check and current complete history, archived with seven images and one table, committed as `0e2fab4`, and pushed to `main`. All prior canonical work/run fields and evidence were preserved or extended by reconciliation. The failed later selection remains in the preparation cache. Pages run [34298706765](https://github.com/YaoJianyu77/dailypaper/actions/runs/34298706765) successfully deployed that article.
- Shared-reader checks cover all 53 archived reports, preserving their complete prose, links, tables, and 58 image references. Browser navigation tests cover root and project base paths, date selection, browser back, adjacent reports, legacy URLs, mobile layout, missing dates/files, changed Markdown hashes, and unsafe embedded text. Today's shared-reader rendering passed desktop/mobile figure controls and all 64 screenshots without overflow or JavaScript errors.
- All 82 offline tests pass, covering settings-only changes, shared full inputs, classic-influence retrieval, permanent aliases/imports, interrupted reservations, rejected evidence, and failed-push recovery against a temporary Git remote. Discovery tests cover HTML challenges, HTTP 429, timeouts, credential-safe diagnostics, official-source fallback, publisher/PACMPL issue verification, DOI/arXiv false collisions, permanent exclusion, retries, and rendered figures/tables. Editorial/browser checks cover bounded same-paper revisions, persistent review rejection, underfill explanations, actual desktop/mobile screenshots, every figure's zoom controls and complete scroll coverage, denied remote resources, and missing rendering receipts. Full-paper checks cover published supplements, original-source hash binding, unavailable appendices, combined document limits, versioned author copies, duplicate-copy suppression, and wrong-work rejection. Runtime/setup checks additionally cover a future flagship, a newly documented reasoning level, ambiguous ordering, unavailable account/model access, parent and descendant substitutions, reroutes, CLI changes, frozen retry configuration, explicit `never` on initial/retried calls, rejected interactive policies and broader sandboxes, read-only effective-turn receipts, empty startup metadata, subagent messages, linked discovery, permission requests, browser preflight failure, timezone preservation, reinstall idempotency, DST, and cross-process exclusion.
- The actual setup command completed twice on the host. Readback confirmed exactly one 07:00 America/New_York job, the existing cron daemon running, and the run lock idle afterward. No daily report was triggered by either installation.
- On 2026-09-08, installed Codex 0.153.4 and the authenticated account dynamically resolved `gpt-6-astra` with `ultra` from current official evidence. The real `--check` execution read both synthetic PDF pages and their random evidence tokens, rendered and viewed a figure, executed native web search, correctly distinguished generation from maintenance, and completed a native subagent whose effective model, reasoning and CLI version matched the parent. This is a capability check, not a scientific report-quality evaluation.
- The updated live `--check` also verified effective `never` approvals and unchanged sandbox permissions for parent and child. Both denied a controlled write outside the workspace and a connection to the controller's test listener; the disposable canary remained unchanged. No escalation or approval was granted.
- Initial `--dry-run` attempts on 2026-09-08 stopped before selection because DBLP returned an HTML challenge and OpenAlex returned HTTP 429. The fixed index check skips those unavailable services in about two seconds; native web discovery subsequently produced 14 controller-verified publications. It also exposed and corrected false DOI/arXiv identity collisions and PACMPL conference-issue matching. No recommendation history was changed.
- Live paper reviews exposed missing experimental conditions, separately published supplements, and an artifact appendix present only in a linked author version. Preparation now supports bounded same-paper revisions and retains all those source documents. The HipKittens acquisition check read all 38 pages across its main paper and final supplement; Flashlight retained both publisher and author copies (39 pages). All four selected papers passed their individual reviews. Final website review exposed unreadable narrow figure previews; the viewer now supports original-size scrolling and zoom, with 64 desktop/mobile screenshots covering all seven figures and the HTML table.
- After an earlier account usage-limit failure, the fresh standalone runtime check passed at 20:31 America/New_York on 2026-09-08. The complete isolated dry-run then resumed the same four approved paper checkpoints, reverified the runtime, and passed final article review and bundle validation at 20:36. It prepared four latest papers and explained the unfilled classic slot under the settings' underfill rule. No production report, asset, or history was written. Readback of 1,129 protected file/cron hashes found only the intentional Pages dependency-install change; production content, history, infrastructure settings, and the schedule were unchanged.
- Desktop (1440 px) and mobile (390 px) browser checks display the fixture's five figures and five HTML tables, with working figure zoom, no page overflow, and no JavaScript errors.
- Before the requested September 8 publication, all 52 existing reports built and their 51 image references were retained; that maintenance pass left production content/state unchanged. The existing Pages workflow installs the browser required by its tests and pins Ubuntu 22.04 after the 24.04 runner rejected Chromium's sandbox initialization. Browser sandboxing stays enabled, with no host security-policy changes. Workflow triggers, permissions and deployment path are unchanged. The earlier cleanup verified all 1,127 tracked content/state, cron-helper and Pages-workflow files against its baseline (`539994c`); this upgrade intentionally replaces the local cron helper and removes the obsolete manual workflows.
- Every prior work identity reaches the model prompts after evidence reconciliation: 622 source records produce 185 distinct work entries without dropping identity tokens. Complete raw evidence remains available to the controller and is preserved by ledger reconciliation.

## Remaining operational limitations

- The isolated end-to-end check passed, as recorded above; future service availability, account quota, and publisher access can still interrupt a run. Missing official evidence, unavailable model access, unverified reasoning support/order, changed protocol/documentation, or an unexpected authorization request stops unattended execution without a downgrade. A conflicting native child can begin before its activity notification arrives; effective-setting checks reject the run before accepting its output into a report.
- Discovery uses configured venues through DBLP/OpenAlex, with native Codex web search when indexed results cannot fill the configured pools. Failed indexes are skipped for the rest of the invocation, and the controller verifies all candidate publication evidence. Service indexing, request budgets, rate limits, inaccessible publisher pages, and ambiguous publication dates can reduce coverage. It is not an exhaustive proceedings crawl.
- Full-paper acquisition requires a readable PDF with extractable text on every page. Scanned pages requiring OCR, inaccessible appendices, oversized inputs, model refusals, and unverified publication or classic-influence evidence stop publication; no abstract fallback is used.
- Scientific claims, topic fit, renamed-work relationships without matching identifiers, and visual fidelity also undergo model review. Deterministic checks cover identity matches, evidence receipts, limits, paths, hashes, and rendering; they cannot prove semantic correctness.
- Interrupted reservations require the original preparation cache. Changed model, reasoning, CLI version, settings, missing runtime receipts, or concurrent Git changes stop safely and require reconciliation and revalidation of the same run. Neither pending reservations nor historical exclusions expire.

The existing Pages workflow builds archived content on pushes to `main`. A successful local build or commit is not evidence of a successful deployment; inspect the corresponding Pages run when deploying.
